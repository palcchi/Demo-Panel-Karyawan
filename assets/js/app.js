const $=s=>document.querySelector(s), $$=s=>document.querySelectorAll(s);
const state={in:null,out:null};
function nowTime(){return new Date().toLocaleTimeString('id-ID',{hour:'2-digit',minute:'2-digit',second:'2-digit'})}
function shortTime(){return new Date().toLocaleTimeString('id-ID',{hour:'2-digit',minute:'2-digit'})}
function updateClock(){const t=nowTime(); $('#clock').textContent=t; $('#clock2').textContent=t}
setInterval(updateClock,1000);updateClock();
$('#fullDate').textContent=new Date().toLocaleDateString('id-ID',{weekday:'long',day:'numeric',month:'long',year:'numeric'});
function demoToast(msg){const el=$('#toast');el.textContent=msg;el.classList.add('show');setTimeout(()=>el.classList.remove('show'),2200)} window.demoToast=demoToast;
function setAttendance(type){if(type==='in'&&!state.in){state.in=shortTime();$('#dashIn').textContent=state.in;$('#attIn').textContent=state.in;$('#attStatus').textContent='Sedang Bekerja';['#attendanceBadge','#attendanceBadge2'].forEach(s=>{$(s).textContent='● Sudah Absen Masuk';$(s).className='badge green'});$('#quickCheckIn').disabled=true;$('#checkInBtn').disabled=true;$('#quickCheckOut').disabled=false;$('#checkOutBtn').disabled=false;demoToast('Absensi masuk berhasil dicatat.')} if(type==='out'&&state.in&&!state.out){state.out=shortTime();$('#dashOut').textContent=state.out;$('#attOut').textContent=state.out;$('#attStatus').textContent='Selesai';['#attendanceBadge','#attendanceBadge2'].forEach(s=>{$(s).textContent='● Absensi Selesai';$(s).className='badge blue'});$('#quickCheckOut').disabled=true;$('#checkOutBtn').disabled=true;demoToast('Absensi pulang berhasil dicatat.')}}
$('#quickCheckIn').onclick=()=>setAttendance('in');$('#checkInBtn').onclick=()=>setAttendance('in');$('#quickCheckOut').onclick=()=>setAttendance('out');$('#checkOutBtn').onclick=()=>setAttendance('out');
function openPage(id){$$('.page').forEach(p=>p.classList.remove('active'));$$('.nav-link').forEach(n=>n.classList.remove('active'));$('#'+id).classList.add('active');document.querySelector(`[data-page="${id}"]`)?.classList.add('active');$('.sidebar').classList.remove('open');history.replaceState(null,'','#'+id);window.scrollTo(0,0)}
$$('.nav-link').forEach(n=>n.onclick=()=>openPage(n.dataset.page));$('#menuBtn').onclick=()=>$('.sidebar').classList.toggle('open');
if(location.hash&&$(location.hash))openPage(location.hash.slice(1));
const modal=$('#requestModal');function openRequest(type='Cuti'){$('#requestType').value=type;modal.classList.add('show')}$('#openRequest').onclick=()=>openRequest();$$('[data-request]').forEach(b=>b.onclick=()=>openRequest(b.dataset.request));$('#closeModal').onclick=()=>modal.classList.remove('show');modal.onclick=e=>{if(e.target===modal)modal.classList.remove('show')};
$('#requestForm').onsubmit=e=>{e.preventDefault();const type=$('#requestType').value,start=$('#requestStart').value,end=$('#requestEnd').value,reason=$('#requestReason').value.trim();if(!start||!end||!reason)return;const fmt=d=>new Date(d+'T00:00:00').toLocaleDateString('id-ID',{day:'numeric',month:'short',year:'numeric'});const tr=document.createElement('tr');tr.innerHTML=`<td>${type}</td><td>${start===end?fmt(start):fmt(start)+' - '+fmt(end)}</td><td>${reason.replace(/[<>]/g,'')}</td><td><span class="badge blue">Menunggu</span></td>`;$('#requestTable').prepend(tr);e.target.reset();modal.classList.remove('show');demoToast('Pengajuan berhasil dikirim.')};
