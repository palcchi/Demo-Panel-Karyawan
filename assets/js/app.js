(()=>{const l=document.createElement('link');l.rel='stylesheet';l.href='assets/css/vape.css';document.head.appendChild(l)})();const $=s=>document.querySelector(s),$$=s=>document.querySelectorAll(s);const state={in:null,out:null,bukdos:150000,kasbon:200000};const rupiah=n=>'Rp'+Number(n).toLocaleString('id-ID');const nowTime=()=>new Date().toLocaleTimeString('id-ID',{hour:'2-digit',minute:'2-digit',second:'2-digit'});const shortTime=()=>new Date().toLocaleTimeString('id-ID',{hour:'2-digit',minute:'2-digit'});function updateClock(){const t=nowTime();$('#clock').textContent=t;$('#clock2').textContent=t}setInterval(updateClock,1000);updateClock();$('#fullDate').textContent=new Date().toLocaleDateString('id-ID',{weekday:'long',day:'numeric',month:'long',year:'numeric'});function demoToast(msg){const el=$('#toast');el.textContent=msg;el.classList.add('show');setTimeout(()=>el.classList.remove('show'),2300)}window.demoToast=demoToast;function setAttendance(type){if(type==='in'&&!state.in){state.in=shortTime();$('#dashIn').textContent=state.in;$('#attIn').textContent=state.in;$('#attStatus').textContent='Sedang Bekerja';['#attendanceBadge','#attendanceBadge2'].forEach(s=>{$(s).textContent='● Sudah Absen Masuk';$(s).className='badge green'});$('#quickCheckIn').disabled=true;$('#checkInBtn').disabled=true;$('#quickCheckOut').disabled=false;$('#checkOutBtn').disabled=false;demoToast('Absensi masuk berhasil dicatat.')}if(type==='out'&&state.in&&!state.out){state.out=shortTime();$('#dashOut').textContent=state.out;$('#attOut').textContent=state.out;$('#attStatus').textContent='Selesai';['#attendanceBadge','#attendanceBadge2'].forEach(s=>{$(s).textContent='● Absensi Selesai';$(s).className='badge blue'});$('#quickCheckOut').disabled=true;$('#checkOutBtn').disabled=true;demoToast('Absensi pulang berhasil dicatat.')}}$('#quickCheckIn').onclick=()=>setAttendance('in');$('#checkInBtn').onclick=()=>setAttendance('in');$('#quickCheckOut').onclick=()=>setAttendance('out');$('#checkOutBtn').onclick=()=>setAttendance('out');function setupReveal(){const nodes=$$('.page.active .card,.page.active .metric,.page.active .day-card,.page.active .page-heading,.page.active .notice-card,.page.active .rule-strip>div,.page.active .limit-card,.page.active .info-banner');nodes.forEach((el,i)=>{el.classList.remove('reveal','visible');el.style.setProperty('--reveal-delay',Math.min(i*42,250)+'ms');el.classList.add('reveal')});const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.1,rootMargin:'0px 0px -30px 0px'});nodes.forEach(el=>io.observe(el))}const sidebar=$('.sidebar'),menuBtn=$('#menuBtn'),header=$('.mobile-header');const menuOverlay=document.createElement('div');menuOverlay.className='menu-overlay';menuOverlay.setAttribute('aria-hidden','true');document.body.appendChild(menuOverlay);const drawerClose=document.createElement('button');drawerClose.className='drawer-close';drawerClose.type='button';drawerClose.setAttribute('aria-label','Tutup menu');drawerClose.textContent='×';sidebar.prepend(drawerClose);menuBtn.setAttribute('aria-expanded','false');function setMenu(open){sidebar.classList.toggle('open',open);menuOverlay.classList.toggle('show',open);document.body.classList.toggle('menu-open',open);menuBtn.setAttribute('aria-expanded',String(open));menuOverlay.setAttribute('aria-hidden',String(!open));if(open)header.classList.remove('nav-hidden')}menuBtn.onclick=()=>setMenu(!sidebar.classList.contains('open'));menuOverlay.onclick=()=>setMenu(false);drawerClose.onclick=()=>setMenu(false);document.addEventListener('keydown',e=>{if(e.key==='Escape'&&sidebar.classList.contains('open'))setMenu(false)});function openPage(id){$$('.page').forEach(p=>p.classList.remove('active'));$$('.nav-link').forEach(n=>n.classList.remove('active'));$('#'+id).classList.add('active');document.querySelector(`[data-page="${id}"]`)?.classList.add('active');setMenu(false);history.replaceState(null,'','#'+id);window.scrollTo({top:0,behavior:'instant'});requestAnimationFrame(setupReveal)}$$('.nav-link').forEach(n=>n.onclick=()=>openPage(n.dataset.page));$$('[data-jump]').forEach(n=>n.onclick=()=>openPage(n.dataset.jump));if(location.hash&&$(location.hash))openPage(location.hash.slice(1));else requestAnimationFrame(setupReveal);function openModal(id){setMenu(false);$('#'+id).classList.add('show')}function closeModal(id){$('#'+id).classList.remove('show')}$('#openPermission').onclick=()=>openModal('permissionModal');$('#openOvertime').onclick=()=>openModal('overtimeModal');$('#openBukdos').onclick=()=>openModal('bukdosModal');$('#openKasbon').onclick=()=>openModal('kasbonModal');$$('[data-close]').forEach(b=>b.onclick=()=>closeModal(b.dataset.close));$$('.modal-backdrop').forEach(m=>m.onclick=e=>{if(e.target===m)m.classList.remove('show')});const fmt=d=>new Date(d+'T00:00:00').toLocaleDateString('id-ID',{day:'numeric',month:'short',year:'numeric'});const today=()=>new Date().toLocaleDateString('id-ID',{day:'numeric',month:'short',year:'numeric'});const clean=s=>String(s).replace(/[<>]/g,'');$('#permissionForm').onsubmit=e=>{e.preventDefault();const type=$('#permissionType').value,start=$('#permissionStart').value,end=$('#permissionEnd').value,reason=clean($('#permissionReason').value.trim());if(!start||!end||!reason)return;const tr=document.createElement('tr');tr.innerHTML=`<td>${type}</td><td>${start===end?fmt(start):fmt(start)+' - '+fmt(end)}</td><td>${reason}</td><td><span class="badge blue">Menunggu</span></td>`;$('#permissionTable').prepend(tr);e.target.reset();closeModal('permissionModal');demoToast('Pengajuan izin dikirim. Hari tidak masuk tetap unpaid.')};$('#overtimeForm').onsubmit=e=>{e.preventDefault();const date=$('#overtimeDate').value,store=clean($('#overtimeStore').value),purpose=clean($('#overtimePurpose').value),note=clean($('#overtimeNote').value.trim());if(!date||!note)return;const tr=document.createElement('tr');tr.innerHTML=`<td>${fmt(date)}</td><td>${store}</td><td>${purpose}: ${note}</td><td>1 Shift</td><td><span class="badge blue">Menunggu</span></td>`;$('#overtimeTable').prepend(tr);e.target.reset();closeModal('overtimeModal');demoToast('Lembur dikirim untuk persetujuan.')};function updateBukdos(){$('#bukdosUsed').textContent=rupiah(state.bukdos);$('#bukdosRemain').textContent=rupiah(250000-state.bukdos)}$('#bukdosForm').onsubmit=e=>{e.preventDefault();const item=clean($('#bukdosItem').value.trim()),qty=Number($('#bukdosQty').value),amount=Number($('#bukdosAmount').value);if(!item||qty<1||amount<1)return;if(state.bukdos+amount>250000){demoToast('Bukdos melebihi limit Rp250.000 bulan ini.');return}state.bukdos+=amount;const tr=document.createElement('tr');tr.innerHTML=`<td>${today()}</td><td>${item}</td><td>${qty}</td><td>${rupiah(amount)}</td>`;$('#bukdosTable').prepend(tr);updateBukdos();e.target.reset();$('#bukdosQty').value=1;closeModal('bukdosModal');demoToast('Bukdos dicatat sebagai potongan gaji.')};$('#kasbonForm').onsubmit=e=>{e.preventDefault();const purpose=clean($('#kasbonPurpose').value.trim()),amount=Number($('#kasbonAmount').value);if(!purpose||amount<1)return;if(state.kasbon+amount>500000){demoToast('Kasbon melebihi limit Rp500.000 bulan ini.');return}const tr=document.createElement('tr');tr.innerHTML=`<td>${today()}</td><td>${purpose}</td><td>${rupiah(amount)}</td><td><span class="badge blue">Menunggu</span></td>`;$('#kasbonTable').prepend(tr);e.target.reset();closeModal('kasbonModal');demoToast('Kasbon dikirim dan menunggu persetujuan.')};let lastY=window.scrollY,acc=0;window.addEventListener('scroll',()=>{if(sidebar.classList.contains('open'))return;const y=window.scrollY,d=y-lastY;if(Math.abs(d)>2){acc+=d;if(y<24){header.classList.remove('nav-hidden');acc=0}else if(acc>28){header.classList.add('nav-hidden');acc=0}else if(acc<-12){header.classList.remove('nav-hidden');acc=0}lastY=y}},{passive:true});